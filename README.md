# Online Judge Executor

# How to run #
```shell
# install docker
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $(whoami)
docker login

pip install -r requirement.txt

python executor_server.py 5000
```
